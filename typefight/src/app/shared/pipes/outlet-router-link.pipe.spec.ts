import { OutletRouterLinkPipe } from './outlet-router-link.pipe';

describe('OutletRouterLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new OutletRouterLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
